import { ReplaceCharactersExceptDashPipe } from './replace-characters-except-dash.pipe';

describe('ReplaceCharactersExceptDashPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceCharactersExceptDashPipe();
    expect(pipe).toBeTruthy();
  });
});
