import { TrimContentPipe } from './trim-content.pipe';

describe('TrimContentPipe', () => {
  it('create an instance', () => {
    const pipe = new TrimContentPipe();
    expect(pipe).toBeTruthy();
  });
});
