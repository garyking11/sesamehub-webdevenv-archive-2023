/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { CopyCssToLocalFileService } from './copy-css-to-local-file.service';

/*describe('Service: CopyCssToLocalFile', () => {
 beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [CopyCssToLocalFileService]
    });
  });

  it('should ...', inject([CopyCssToLocalFileService], (service: CopyCssToLocalFileService) => {
    expect(service).toBeTruthy();
  }));
});*/
