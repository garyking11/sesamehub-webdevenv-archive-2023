/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
//import { CreateSitemapxmlService } from './create-sitemapxml.service';

/*describe('Service: CreateSitemapxml', () => {
 beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [CreateSitemapxmlService]
    });
  });

  it('should ...', inject([CreateSitemapxmlService], (service: CreateSitemapxmlService) => {
    expect(service).toBeTruthy();
  }));
});*/
