/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { GetCurrentDomainUrlsService } from './get-current-domain-urls.service';

describe('Service: GetCurrentDomainUrls', () => {
 /* beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [GetCurrentDomainUrlsService]
    });
  });

  it('should ...', inject([GetCurrentDomainUrlsService], (service: GetCurrentDomainUrlsService) => {
    expect(service).toBeTruthy();
  }));*/
});
