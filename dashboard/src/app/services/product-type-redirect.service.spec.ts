/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { ProductTypeRedirectService } from './product-type-redirect.service';

describe('Service: ProductTypeRedirect', () => {
 /* beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [ProductTypeRedirectService]
    });
  });

  it('should ...', inject([ProductTypeRedirectService], (service: ProductTypeRedirectService) => {
    expect(service).toBeTruthy();
  }));*/
});
